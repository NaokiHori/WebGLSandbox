{
  "name": "webgl-sandbox",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "check": "tsc",
    "test": "vitest run",
    "build": "vitest run && vite build",
    "dev": "vite",
    "prepare": "bash npm-scripts/prepare.sh"
  },
  "lint-staged": {
    "*": [
      "eslint --cache --fix",
      "prettier . --write"
    ]
  },
  "devDependencies": {
    "eslint": "^9.17.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.2.11",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.2",
    "vite": "^6.0.6",
    "vitest": "^2.1.8"
  }
}
