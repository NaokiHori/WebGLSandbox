{
  "name": "webgl-sandbox",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "check": "tsc",
    "test": "vitest run",
    "build": "vitest run && vite build",
    "dev": "vite",
    "prepare": "bash npm-scripts/prepare.sh"
  },
  "lint-staged": {
    "*": [
      "prettier . --write",
      "eslint --cache --fix"
    ]
  },
  "devDependencies": {
    "eslint": "^9.14.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3",
    "vite": "^5.4.11",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.15.0",
    "vitest": "^2.1.8"
  }
}
